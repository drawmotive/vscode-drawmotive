node_modules
.vscode-test/
*.vsix
.env

# Tracked release policy; generated archives/receipts remain ignored.
.release/*
!.release/
!.release/target.json
!.release/check.cjs

# Build outputs come from the locked npm editor package, never private binaries.
media/
dist/
out/
