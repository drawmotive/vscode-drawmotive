{
  "name": "vscode-drawmotive",
  "displayName": "DrawMotive - Diagram & Flowchart Editor",
  "description": "Create technical diagrams, flowcharts, and visual documentation with embedded PNG files. Draw offline, version control friendly.",
  "version": "0.2.1",
  "publisher": "drawmotive",
  "repository": {
    "type": "git",
    "url": "https://github.com/drawmotive/vscode-drawmotive.git"
  },
  "bugs": {
    "url": "https://github.com/drawmotive/vscode-drawmotive/issues"
  },
  "homepage": "https://docs.drawmotive.com",
  "icon": "icon.png",
  "engines": {
    "vscode": "^1.95.0",
    "node": ">=22"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "keywords": [
    "diagram",
    "flowchart",
    "drawing",
    "uml",
    "technical diagram",
    "architecture diagram",
    "png",
    "visual",
    "documentation",
    "offline",
    "whiteboard",
    "sketch"
  ],
  "activationEvents": [
    "onCustomEditor:drawmotive.editor"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "customEditors": [
      {
        "viewType": "drawmotive.editor",
        "displayName": "DrawMotive Editor",
        "selector": [
          {
            "filenamePattern": "*.draw.png"
          }
        ],
        "priority": "default"
      }
    ],
    "commands": [
      {
        "command": "drawmotive.newDrawing",
        "title": "New Diagram",
        "category": "DrawMotive"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run assets:prepare && webpack",
    "watch": "npm run assets:prepare && webpack --watch",
    "package": "node .release/check.cjs --installed && npm run compile -- --mode production --devtool hidden-source-map",
    "lint": "eslint src",
    "vsce:package": "node .release/check.cjs --ready && vsce package --no-dependencies",
    "vsce:publish": "node .release/check.cjs --ready && vsce publish --no-dependencies",
    "vsce:publish-prerelease": "node .release/check.cjs --ready && vsce publish --pre-release --no-dependencies",
    "release:check": "node .release/check.cjs --ready",
    "assets:prepare": "node scripts/prepare-editor.cjs",
    "test": "tsc && node --test --test-concurrency=1 test/*.test.cjs",
    "test:browser": "npm run compile && tsc && node --test --test-concurrency=1 test/*.browser.cjs"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.95.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.2.1",
    "eslint": "^8.54.0",
    "globals": "^15.14.0",
    "playwright": "1.61.0",
    "ts-loader": "^9.5.1",
    "typescript": "^5.3.3",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "@drawmotive/editor": "0.2.1"
  }
}
